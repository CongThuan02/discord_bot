# Token bot Discord — lấy từ https://discord.com/developers/applications
DISCORD_TOKEN=your_bot_token_here

# (Tùy chọn) Thư mục lưu file user upload + config.
# Khi deploy Railway, đặt = đường dẫn mount của Volume (vd: /data).
# Để trống khi chạy local.
# DATA_DIR=/data
